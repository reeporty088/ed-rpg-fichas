import { emptyState } from '../../ui/components/elements.js';
export function renderCampaignsModule() {
  return emptyState('Campaigns', 'Este módulo está preparado na fundação da V5. Sua funcionalidade será implementada em um PR futuro.');
}
