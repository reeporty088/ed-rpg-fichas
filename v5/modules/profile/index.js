import { emptyState } from '../../ui/components/elements.js';
export function renderProfileModule() {
  return emptyState('Profile', 'Este módulo está preparado na fundação da V5. Sua funcionalidade será implementada em um PR futuro.');
}
