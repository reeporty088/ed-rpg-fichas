import { emptyState } from '../../ui/components/elements.js';
export function renderCompendiumModule() {
  return emptyState('Compendium', 'Este módulo está preparado na fundação da V5. Sua funcionalidade será implementada em um PR futuro.');
}
